{
  "name": "mehen",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "tauri": "tauri",
    "tauri-dev": "tauri dev",
    "typecheck": "tsc -b --force",
    "test:rust": "cargo test --workspace",
    "bootstrapper:build": "cargo build --manifest-path src-tauri/bootstrapper/Cargo.toml --release",
    "bootstrapper:dev": "cargo run --manifest-path src-tauri/bootstrapper/Cargo.toml --release -- --dry-run",
    "bootstrapper:dev:error": "cargo run --manifest-path src-tauri/bootstrapper/Cargo.toml --release -- --dry-run --fail",
    "bootstrapper:updater:dev": "cargo run --manifest-path src-tauri/bootstrapper/Cargo.toml --release -- --updating --dry-run",
    "bootstrapper:updater:dev:error": "cargo run --manifest-path src-tauri/bootstrapper/Cargo.toml --release -- --updating --dry-run --fail"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.3.0",
    "@fontsource/geist-mono": "^5.3.0",
    "@fontsource/sora": "^5.3.0",
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "@tauri-apps/plugin-opener": "^2.5.5",
    "lucide-react": "^1.47.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@tauri-apps/cli": "^2.11.5",
    "@types/node": "^26.6.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
